import { Grid } from "@mui/material";
import styled from "styled-components";

export const AccessDeniedPageContainer = styled(Grid)`
    margin-top: 5rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 1rem;
`;